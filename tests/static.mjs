import{readFileSync}from"node:fs";import assert from"node:assert/strict";const d=readFileSync("Dockerfile","utf8"),p=readFileSync("bootstrap.php","utf8"),r=readFileSync("README.md","utf8");assert.match(d,/xbackbone:3\.8\.2@sha256:[a-f0-9]{64}/);assert.doesNotMatch(d,/:latest/);assert.match(p,/UserRepository::make/);assert.match(p,/is_file\(\$configPath\)/);assert.match(p,/sqlite:/);assert.match(r,/registration remains disabled/i);assert.match(r,/do not scale horizontally/i);console.log("static template checks passed");
